# odin-calculator

QOL features:

1. history display to show context of previous actions
2. size adjust when value over flows

Extra from Odin Project

3. button for decimal .
4. backspace

        If on result screen, clears screen
5. keyboard support
    
        As is (1 to 0, -, +, /, x, .)

        = (Enter, =)
        <= (Backspace)
        C (c key)
        +- (n key)
        M (m key)

Extra

6. +-
7. Memory

    Use button color to show storage status

    Stores whatever is on display
    
        if empty store 0

    When full

        Only outputs when in display value is empty

        Otherwise stores

